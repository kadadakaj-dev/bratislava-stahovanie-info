import { Translations, View } from '../App';
import { Post } from '../types';

export const seoService = {
  updateSeoTags: (_view: View, _t: Translations, _post?: Post): void => {
    // Update SEO tags logic
  },
};
